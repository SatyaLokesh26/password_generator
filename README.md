# password_generator
This mini project generates a strong random password in Java. The user enters the desired length, and the program combines uppercase, lowercase, numbers, and special characters to create it. It demonstrates the use of strings, loops, and the Random class for secure password generation.
